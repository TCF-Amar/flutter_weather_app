import 'package:flutter/material.dart';

class AppColors {
  // ─────────── Base Colors ───────────
  static const Color transparent = Colors.transparent;
  static const Color white = Colors.white;
  static const Color black = Colors.black;

  // ─────────── Gradient Colors ───────────
  static const Color grigent1 = Color(0xFF3C6FD1);
  static const Color grigent2 = Color(0xFF7CA9ff);
  static const Color grigent1Dark = Color(0xFF1A2B5C);
  static const Color grigent2Dark = Color(0xFF2D4A8E);

  // ─────────── Common Colors ───────────
  static const Color grey = Colors.grey;
  static const Color red = Colors.red;
  static const Color green = Colors.green;
  static const Color blue = Colors.blue;
  static const Color yellow = Colors.yellow;
  static const Color orange = Colors.orange;
  static const Color purple = Colors.purple;
  static const Color lightGrey = Color(0xFFE0E0E0);
  static const Color indigo = Colors.indigo;
  static const Color orangeAccent = Colors.orangeAccent;
  static const Color blueAccent = Colors.blueAccent;
  static const Color blueGrey = Colors.blueGrey;

  // ─────────── Light Theme Colors ───────────

  // Primary colors
  static const Color primary = Color.fromARGB(255, 27, 47, 230);
  static const Color primaryLight = Color(0xFF424B97);
  static const Color primaryDark = Color(0xFF0D123C);

  // Accent colors
  static const Color accent = Color(0xFFC33764);
  static const Color secondary = Color(0xFFA6E3E9);

  // Background colors
  static const Color background = Color(0xFFF5F7FA);
  static const Color surface = Colors.white;

  // Text colors
  static const Color textPrimary = Color(0xFF2D3436);
  static const Color textSecondary = Color(0xFF636E72);
  static const Color textLight = Colors.white;

  // Status colors
  static const Color success = Color(0xFF00B894);
  static const Color error = Color(0xFFD63031);
  static const Color warning = Color(0xFFFDCB6E);

  // ─────────── Dark Theme Colors ───────────

  // Dark backgrounds
  static const Color backgroundDark = Color(0xFF0D1117);
  static const Color surfaceDark = Color(0xFF161B22);
  static const Color surfaceVariantDark = Color(0xFF1C2128);

  // Dark text colors
  static const Color textLightPrimary = Color(0xFFE6EDF3);
  static const Color textLightSecondary = Color(0xFF8B949E);

  // Dark accent colors
  static const Color secondaryDark = Color(0xFF58A6FF);
  static const Color accentDark = Color(0xFFFF7B72);

  // Dark status colors
  static const Color successDark = Color(0xFF3FB950);
  static const Color errorDark = Color(0xFFFF7B72);
  static const Color warningDark = Color(0xFFD29922);

  // Dark grey shades
  static const Color greyDark = Color(0xFF30363D);
  static const Color greyLightDark = Color(0xFF484F58);

  // ─────────── Weather Specific Colors ───────────

  // Light theme weather colors
  static const Color sunny = Color(0xFFFFD93D);
  static const Color rainy = Color(0xFF4A90E2);
  static const Color cloudy = Color(0xFF90A4AE);

  // Dark theme weather colors
  static const Color sunnyDark = Color(0xFFFFC107);
  static const Color rainyDark = Color(0xFF58A6FF);
  static const Color cloudyDark = Color(0xFF6E7681);
}
